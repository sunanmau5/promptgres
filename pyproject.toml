[project]
name = "cursor-eda"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "psycopg[binary]>=3.2.0",
    "python-dotenv>=1.0.0",
    "scikit-learn>=1.6.1",
    "xgboost>=3.0.0",
    "folium",
]
